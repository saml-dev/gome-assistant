# Example configuration for running `cmd/generate`. If you want to
# adapt this example for your own setup, you should fill in your own
# values here and run `go generate`. See `README.md` for more
# information.

url: "http://192.168.1.123:8123"
ha_auth_token: ""
home_zone_entity_id: "zone.home"
include_domains: ["zone", "binary_sensor", "light"]
